import { LingerMainPage } from '../components/linger-main-page'; // Adjust the import path based on the actual component name and package

const HomePage = () => {
  return (
    <main>
      <LingerMainPage />
    </main>
  );
};

export default HomePage;